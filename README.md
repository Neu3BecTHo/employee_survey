# Employee Survey Web App

Веб-приложение для проведения внутренних опросов сотрудников с ролевой системой доступа.

## 📋 Описание

Система позволяет администраторам создавать опросы, а сотрудникам проходить их и просматривать свои ответы. Приложение реализовано на Go (backend) и PostgreSQL (база данных) с современным frontend на JavaScript.

## 🚀 Функциональность

### Для администраторов:
- ✅ Создание и редактирование опросов
- ✅ Добавление вопросов с вариантами ответа
- ✅ Управление статусами опросов (draft/open/closed)
- ✅ Просмотр агрегированных результатов
- ✅ Полный доступ ко всем данным

### Для сотрудников:
- ✅ Просмотр доступных опросов
- ✅ Прохождение опросов
- ✅ Просмотр своих ответов
- ✅ Защита от повторного прохождения

### Технические особенности:
- ✅ Ролевая система (admin/employee)
- ✅ Валидация обязательных вопросов
- ✅ Защита от дублирования ответов
- ✅ RESTful API с правильными HTTP методами
- ✅ Docker контейнеризация
- ✅ Миграции базы данных

## 🛠 Технологический стек

### Backend:
- **Go 1.24** - основной язык программирования
- **PostgreSQL 15** - база данных
- **Gorilla Mux** - HTTP роутер
- **database/sql** - работа с БД
- **golang-migrate** - миграции

### Frontend:
- **HTML5** - семантическая разметка
- **CSS3** - стилизация
- **JavaScript ES6+** - логика интерфейса
- **Fetch API** - работа с backend

### Инфраструктура:
- **Docker** - контейнеризация
- **Docker Compose** - оркестрация
- **JSONB** - хранение вариантов ответа

## 📁 Структура проекта

```
employee_survey/
├── main.go                 # Главный файл приложения
├── handlers.go             # HTTP обработчики
├── internal/
│   └── models.go          # Модели данных
├── migrations/             # Миграции БД
├── static/
│   ├── css/               # Стили
│   ├── js/                # JavaScript файлы
│   └── *.html             # HTML страницы
├── docker-compose.yml     # Docker конфигурация
├── Dockerfile             # Docker образ
└── README.md              # Этот файл
```

## 🚀 Быстрый старт

### Требования:
- Docker и Docker Compose
- Go 1.24+ (для локальной разработки)
- PostgreSQL 15+ (для локальной разработки)

### Запуск через Docker:

1. Клонируйте репозиторий:
```bash
git clone <repository-url>
cd employee_survey
```

2. Запустите приложение:
```bash
sudo docker-compose up --build
```

3. Откройте в браузере:
```
http://localhost:8080
```

### Локальная разработка:

1. Установите зависимости:
```bash
go mod download
```

2. Запустите PostgreSQL:
```bash
sudo systemctl start postgresql
```

3. Выполните миграции:
```bash
go run main.go migrate up
```

4. Запустите приложение:
```bash
go run main.go handlers.go
```

## 📊 API Эндпоинты

### Публичные эндпоинты:
- `GET /users` - список пользователей
- `GET /surveys` - список опросов
- `GET /surveys/{id}` - детали опроса
- `POST /surveys/{id}/responses` - отправка ответов
- `GET /surveys/{id}/results` - результаты опроса
- `GET /surveys/my` - мои ответы

### Администраторские эндпоинты:
- `POST /surveys` - создание опроса
- `PUT /surveys/{id}` - обновление опроса
- `POST /surveys/{id}/questions` - добавление вопроса
- `POST /surveys/{id}/open` - открытие опроса
- `POST /surveys/{id}/close` - закрытие опроса

## 🔐 Ролевая система

### Администратор (admin):
- Полный доступ ко всем функциям
- Создание и управление опросами
- Просмотр всех результатов

### Сотрудник (employee):
- Просмотр открытых опросов
- Прохождение опросов
- Просмотр только своих ответов

## 📝 Примеры запросов

### Получение списка пользователей:
```bash
curl -H "X-User-Id: 1" http://localhost:8080/users
```

### Создание опроса (только admin):
```bash
curl -X POST \
  -H "Content-Type: application/json" \
  -H "X-User-Id: 1" \
  -d '{"title":"Новый опрос","description":"Описание опроса"}' \
  http://localhost:8080/surveys
```

### Отправка ответов:
```bash
curl -X POST \
  -H "Content-Type: application/json" \
  -H "X-User-Id: 2" \
  -d '{"answers":[{"question_id":1,"value":"Отлично"}]}' \
  http://localhost:8080/surveys/1/responses
```

## 🧪 Тестирование

### Backend тесты:
```bash
go test -v
```

### Ручное тестирование:
1. Создание опроса администратором
2. Прохождение опроса сотрудником
3. Просмотр результатов администратором

## 📋 База данных

### Основные таблицы:
- `users` - пользователи
- `surveys` - опросы
- `survey_questions` - вопросы опросов
- `survey_responses` - ответы пользователей
- `survey_answers` - конкретные ответы

### Индексы:
- `survey_questions(survey_id)`
- `survey_responses(survey_id, user_id)` - уникальный
- `survey_answers(response_id, question_id)`

## 🔧 Конфигурация

### Переменные окружения:
- `DB_HOST` - хост БД (по умолчанию: localhost)
- `DB_PORT` - порт БД (по умолчанию: 5432)
- `DB_USER` - пользователь БД (по умолчанию: survey_user)
- `DB_PASSWORD` - пароль БД (по умолчанию: survey_pass)
- `DB_NAME` - имя БД (по умолчанию: survey_app)

## 🚨 Валидация

### Обязательные поля:
- Название опроса
- Текст вопроса
- Ответы на обязательные вопросы

### Ограничения:
- Один пользователь не может отвечать дважды на один опрос
- Нельзя отправить пустые ответы
- Вопросы можно добавлять только в статусе "draft"

## 📱 Frontend страницы

- `/` - главная страница со списком опросов
- `/login` - выбор пользователя для входа
- `/surveys/{id}/take` - прохождение опроса
- `/surveys/my` - мои ответы (сотрудник)
- `/admin/surveys` - управление опросами (администратор)
- `/admin/surveys/{id}` - редактирование опроса
- `/surveys/{id}/results` - результаты опроса

## 🤝 Вклад

1. Fork проекта
2. Создайте feature branch
3. Внесите изменения
4. Push в branch
5. Создайте Pull Request

## 📄 Лицензия

MIT License

## 📞 Поддержка

Для вопросов и предложений создайте Issue в репозитории.

---

**Разработано с ❤️ для внутренних опросов сотрудников**
